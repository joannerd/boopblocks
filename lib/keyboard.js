// // // handle the logic for user interaction updating the necessary Soundwave objects for render.

// let newDiv = document.createElement("div");
// newDiv.setAttribute("id", `synth${i}`);
// newDiv.setAttribute("class", "synth");

// document.getElementById("synth-container").appendChild(newDiv);